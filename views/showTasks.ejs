<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title>Show Tasks</title>
  <link rel="stylesheet" type="text/css" href="/css/stylelogin.css">
  <div class="main">
    <div class="logo">
      <img src="/images/logo.png">
    </div>
</head>

<body>
  <br>
  <br>
  <% if(category == 'manager'){ %>
    <h2><a href="/manage/tasks/create" style="color:white;"">Create new task</a></h2>
  <% } %>
  <div class="login" style="margin: 20vh auto; width: 80%; padding: 30px 2px; padding-top: 10px;">

    <form id="login" style="width: 100%; height: 100%;">
      <h1 style="text-align: center;">Your tasks</h1>
      <table style="width:100%">
        <thead>
          <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Due Date</th>
            <th>Manager email</th>
            <th>Team name</th>
            <th>Points</th>
            <th>Rewards</th>
            <th>Completed?</th>
          </tr>
        </thead>
        <% if(category == 'employee'){ %>
          <% if(typeof resultTask != 'undefined' && typeof resultTeam != 'undefined' ){ %>
            <% for(let i=0;i<(resultTask.length);i++){ %>
            <tr>
            <td><%= resultTask[i].title %></td>
            <td><%= resultTask[i].description %></td>
            <td><%= resultTask[i].dueDate %></td>
            <td><%= resultTask[i].manager %></td>
            <td><%= resultTeam.name %></td>
            <td><%= resultTask[i].points %></td>
            <td><%= resultTask[i].rewards %></td>
            <td><%= resultTask[i].completed %></td>
            </tr>
            <% } %>
          <% } %>
        <% } %>

        <% if(category == 'manager'){ %>
          <tr>
            <th>
            Title
            </th>
            <th>description</th>
            <th>due date</th>
            <th>team name</th>
            <th>points</th>
            <th>Status</th>
          </tr>
          <% if(typeof resultTask != 'undefined' || teamName!=null){ %>
            <% for(let i=0;i<(resultTask.length);i++){ %>

              <tr>
              <td><%= resultTask[i].title %></td>
              <td><%= resultTask[i].description %></td>
              <td><%= resultTask[i].dueDate %></td>
              <td><%= teamName[i] %></td>
              <td><%= resultTask[i].points %></td>
              <td><%= resultTask[i].rewards %></td>
              <td><%= resultTask[i].completed %></td>
              <% if(resultTask[i].completed==false){ %><td><a href="/manage/tasks/submit/<%=resultTask[i].team%>/<%=resultTask[i].title%>"><h6>done?</h6></a></td><% } %>
              </tr>
            <% } %>
          <% } %>
        <% } %>
      </table>
    </form>
  </div>
</body>
</html>