<table>
    <% results.forEach(result=>{ %>
        <tr>
            <td><%= result.name %></td>
            <td><%= result.members %></td>
            <td><%= result.manager %></td>
            <td><%= result.points %></td>
        </tr>
    <% }) %>
</table>